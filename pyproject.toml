[project]
name = "unmute"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pipecat-ai[kokoro,local-smart-turn,mcp,mlx-whisper,openai,silero,webrtc,whisper]==1.7.0",
    "python-dotenv>=1.2.3",
]
